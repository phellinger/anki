# Domains
FRONTEND_DOMAIN=example.com
API_DOMAIN=api.example.com

# MySQL
MYSQL_ROOT_PASSWORD=your_secure_password
MYSQL_DATABASE=decks

# Backend
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=your_secure_password
DB_NAME=decks
DB_PORT=3306
SERVER_PORT=5193
FRONTEND_URL=https://${FRONTEND_DOMAIN}

# Frontend (CRA). Capacitor native builds need this full API base URL—no dev proxy on device.
REACT_APP_API_URL=https://${API_DOMAIN}

# Email (backend — OTP / registration). Without SMTP_HOST, codes are only logged to console.
# MAIL_FROM=
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# OTP_PEPPER=change-to-a-long-random-string